---
id: dave-component-attachment-api
project: dave-the-diver
title: 컴포넌트 부착·조회 API와 소유자 연결
description: 기존 Prototype 기반 컴포넌트 구성을 유지하면서 반복적인 Clone·캐스팅·등록을 템플릿 API로 정리하고 소유자 연결과 해제 순서를 보강한 작업
slug: /dave-the-diver/component-attachment-api
order: 110
featured: false
tags:
  - C++
  - Components
  - Game Framework
---

# 컴포넌트 부착·조회 API와 소유자 연결

## 반복적인 부착 코드를 공통화

게임 객체는 Prototype에서 컴포넌트를 복제하고, 원하는 타입으로 캐스팅한 뒤 객체의 컴포넌트 맵에 등록하는 구조였다. Buffer·Texture·Transform마다 같은 준비 코드를 반복했다.

기존 Prototype 구조에서 반복되던 부착 코드를 `AddComponent<T, ID>()`로 묶었다. 타입 지정 조회 API와 컴포넌트의 소유 객체 접근도 추가했다.

기존 코드는 다음과 같은 형태였다. 아래 예시는 당시 Buffer 부착 코드를 간추린 것이다.

```cpp
CComponent* pComponent = m_pBufferCom =
    dynamic_cast<CRcTex*>(
        CProtoMgr::GetInstance()->Clone_Prototype(L"Proto_RcTex"));
if (pComponent == nullptr)
    return E_FAIL;

m_mapComponent[ID_STATIC].insert({L"Com_Buffer", pComponent});
```

공통화 후 파생 객체에서는 타입·갱신 그룹·Prototype 이름·객체 내부 이름을 지정한다.

```cpp
const HRESULT result = AddComponent<Engine::CRcTex, ID_STATIC>(
    L"Proto_RcTex", L"Com_Buffer", &m_pBufferCom);
if (FAILED(result))
    return E_FAIL;
```

`Proto_RcTex`는 복제할 원본을 찾는 이름이고, `Com_Buffer`는 해당 객체 안에서 조회할 이름이다. 마지막 출력 인자는 선택 사항이며, 등록한 컴포넌트와 같은 포인터를 돌려준다. 별도의 소유권이나 참조 카운트를 추가하는 반환은 아니다.

## 부착과 조회의 실제 동작

`AddComponent()`는 protected 함수로 파생 객체의 초기화 코드에서 사용한다. 내부 순서는 **Prototype 복제 → dynamic_cast → 출력 포인터 설정 → 소유자 연결 → 맵 등록**이다. 복제 또는 타입 변환 결과가 null이면 `E_FAIL`을 반환한다.

`GetComponent()`는 public 함수다. 지정한 그룹에서 이름으로 찾고, `dynamic_cast` 결과를 반환한다. 이름이 없거나 타입이 다르면 null이므로 호출자가 검사해야 한다.

```cpp
auto* pTransform = GetComponent<Engine::CTransform, ID_DYNAMIC>(
    L"Com_Transform");
if (pTransform == nullptr)
    return E_FAIL;
```

템플릿으로 호출부의 타입 표기와 반복 코드를 정리하고, 실제 타입 검사는 `dynamic_cast`로 수행한다.

`ID_STATIC`과 `ID_DYNAMIC`은 기본 컴포넌트 갱신 루프의 참여 여부를 구분한다. 기본 Update·LateUpdate는 Dynamic 그룹을 순회한다. Static이라는 이름이 컴포넌트의 불변성이나 인스턴스 공유를 뜻하지는 않으며, 두 그룹 모두 부착 시 Prototype을 복제한다.

## 소유자 연결과 수명

컴포넌트에 `m_pGameObject`와 `Get_GameObject()`·`Set_GameObject()`를 추가했다. 부착 시 `Set_GameObject(this)`를 호출해 컴포넌트가 자신을 사용하는 객체에 접근할 수 있게 했다.

복사 생성자에서는 원본의 소유자 주소를 복사하지 않고 null로 초기화한다. 새 복제본은 부착 대상이 결정된 뒤 해당 객체와 연결된다.

객체의 `Free()`에서는 등록된 모든 컴포넌트의 소유자 포인터를 먼저 null로 만든 뒤 컴포넌트를 해제하고 맵을 비운다. 컴포넌트 정리 중 소유 객체를 계속 참조하는 경로를 끊기 위한 순서다.

이 연결은 raw pointer이며 소유 객체의 수명을 연장하지 않는다. 조회·출력 포인터 역시 객체가 관리하는 컴포넌트에 대한 접근 수단이므로, 외부에서 장기간 보관하거나 임의로 해제해도 안전하다는 의미는 아니다.

## 구현 결과 {/* #성과와-남은-보강점 */}

호출부가 직접 수행하던 복제·캐스팅·등록을 한곳으로 모으고, 부착 시 소유자 연결이 함께 이루어지도록 정리했다. 작은 API 보강이지만 객체별 준비 코드의 중복을 줄이고 사용 방식을 통일하는 역할을 했다.

이 API는 객체 생성 시 컴포넌트를 부착하는 흐름을 공통화한다. 컴포넌트 의존성 자동 해결이나 등록 실패 시 일괄 복구는 지원하지 않는다.
